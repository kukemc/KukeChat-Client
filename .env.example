# KukeChat 客户端配置模板
#
# 复制本文件为 .env 后填写。仓库不内置任何后端地址，
# 必填项缺失时 npm run dev / build 会直接失败并给出提示。
#
# 三端（扩展 / 桌面 / 移动）共用这一份配置。

# ---------------------------------------------------------------- 必填

# 后端 HTTP API 根地址，不要带 /api/v1，也不要带结尾斜杠。
VITE_API_BASE_URL=https://chat.example.com

# 实时消息 WebSocket 地址。
VITE_WS_URL=wss://chat.example.com/ws

# ---------------------------------------------------------------- 选填

# 安全登录弹窗回传 postMessage 时的可信来源。
# 默认取 VITE_API_BASE_URL 的 origin，登录页与 API 不同域时才需要设置。
# VITE_SECURE_LOGIN_ORIGIN=https://chat.example.com

# Android 客户端检查更新用的元数据地址，需返回 { mobile_version, android_url }。
# 默认为 <VITE_API_BASE_URL>/desktop-update.json。
# VITE_MOBILE_UPDATE_URL=https://chat.example.com/desktop-update.json

# 已发布的扩展文件地址，展示在客户端「如何接入机器人」弹窗里，
# 供用户在 CCW/Gandi 中加载。留空则隐藏该入口。
# VITE_EXTENSION_ASSET_URL=https://assets.example.com/extension/kukechat

# 机器人 API 文档地址（对应本仓库 docs/bot-api.md），
# 用于 Scratch 积木面板的「查看文档」入口。留空则不显示。
# VITE_BOT_API_DOCS_URL=https://chat.example.com/docs/bot-api.md

# ---------------------------------------------------------------- 桌面端
#
# Tauri 桌面端的「检查更新」地址由 Rust 侧在编译期读取，
# 不走 VITE_ 前缀。打包前在同一个终端里设置即可：
#
#   PowerShell:  $env:KUKECHAT_UPDATE_METADATA_URL = 'https://chat.example.com/desktop-update.json'
#   bash:        export KUKECHAT_UPDATE_METADATA_URL='https://chat.example.com/desktop-update.json'
#
# 不设置时桌面端「检查更新」会直接跳过。
